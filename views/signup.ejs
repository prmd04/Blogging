<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/output.css" />
    <title>Document</title>
  </head>
  <body class="bg-gray-900">
    <div class="min-h-screen flex items-center justify-center p-4">
      <div class="w-full max-w-md">
        
        <h1 class="text-4xl font-extrabold text-white text-center mb-8">
          Create Your Account
        </h1>

        <form
          action="/register"
          method="POST"
          class="bg-gray-800 shadow-2xl rounded-xl p-8 space-y-6 mb-6"
        >
          <input
            type="name"
            name="name"
            placeholder="Name"
            class="w-full text-white px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-300 placeholder-gray-400"
          />
          <input
            type="email"
            name="email"
            placeholder="Email"
            class="w-full text-white px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-300 placeholder-gray-400"
          />
          <input
            type="password"
            name="password"
            placeholder="Password"
            class="w-full text-white px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-300 placeholder-gray-400"
          />
          <input
            type="submit"
            value="Signup"
            class="w-full px-4 py-3 bg-blue-600 text-white font-semibold rounded-lg shadow-md hover:bg-blue-700 focus:outline-none focus:ring-4 focus:ring-blue-500 focus:ring-opacity-50 transition duration-300 cursor-pointer uppercase tracking-wider"
          />
        </form>

        <div class="text-center mt-4">
          <p class="text-gray-400 text-base">
            Already have an account? 
            <a 
              href="/login" 
              class="text-blue-500 font-semibold hover:text-blue-400 transition duration-200"
            >
              click here
            </a>
          </p>
        </div>
        <%if(message){%>
          <p
            id="flash-message"
            class="text-lg font-semibold text-green-400 bg-green-900/50 p-3 rounded-lg border border-green-600 shadow-md text-center mt-6"
          >
            <%= message %>
          </p>
        <%} %>
        
      </div>
    </div>
  </body>
  <script>
    const msg = document.getElementById("flash-message");
    if (msg) {
      setTimeout(() => {
        msg.remove();
      }, 3500);
    }
  </script>
</html>